name: Update profile card

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - "dark_mode.svg"
      - "light_mode.svg"
      - ".cache/**"

permissions:
  contents: write

concurrency:
  group: profile-card
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - name: Generate SVGs
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: python generate.py

      - name: Commits if changed
        run: |
          git config user.name  "gbrlevi"
          git config user.email "66504106+gbrlevi@users.noreply.github.com"
          git add dark_mode.svg light_mode.svg .cache
          if git diff --quiet --cached; then
            echo "Nada mudou."
          else
            git commit -m "chore: update profile card [skip ci]"
            git push
          fi
